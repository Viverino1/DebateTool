import { useEffect } from "react";
import { useLocation } from "react-router-dom"

export default function TeamInvite(){

  return(
    <div className="w-full h-full">
      <div>You've been invited to join team {}</div>
    </div>
  )
}